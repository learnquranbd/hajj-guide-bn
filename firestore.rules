rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // প্রতিটি হাজী শুধু নিজের অগ্রগতি পড়তে ও লিখতে পারবেন
    match /pilgrims/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
